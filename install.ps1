# install.ps1 — Claude Agents Framework (legacy installer)
#
# The recommended install method is now the Claude Code plugin system:
#   /plugin install claude-agents-framework
#
# This script is kept for users who prefer manual install to ~/.claude/.
# It copies files to ~/.claude/ (the v1 approach).
#
# After plugin install, register the auto-classification hook:
#   .\scripts\setup-hook.ps1

Write-Host ""
Write-Host "Claude Agents Framework"
Write-Host "======================="
Write-Host ""
Write-Host "Recommended install: Claude Code plugin system"
Write-Host "  /plugin install claude-agents-framework"
Write-Host ""
Write-Host "After plugin install, register the hook:"
Write-Host "  .\scripts\setup-hook.ps1"
Write-Host ""
Write-Host "For legacy manual install (copies to ~/.claude/):"
Write-Host "  See docs/INSTALL_LEGACY.md"
Write-Host ""
